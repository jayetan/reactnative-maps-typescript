import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  view: {
    top: 0,
    zIndex: 1,
    height: '100%',
    width: '100%',
  },
  card: {
    height: '100%',
    borderRadius: 0,
  },
  section: {
    paddingBottom: 100,
  },
  item: {
    backgroundColor: '#fff',
  },
});

export default styles;
