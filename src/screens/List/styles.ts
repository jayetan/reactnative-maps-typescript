import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  section: {
    paddingBottom: 100,
  },
  cover: {
    width: '30%',
    height: 100,
  },
});

export default styles;
